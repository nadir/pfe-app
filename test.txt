import { StatusBar } from "expo-status-bar";
import { StyleSheet, View, Text } from "react-native";
import {
  Button,
  Provider as PaperProvider,
  TextInput,
} from "react-native-paper";

export default function App() {
  return (
    <PaperProvider>
      <View style={styles.container}>
        {/* username and password input fields with login button */}
        <TextInput
          label="Username"
          mode="outlined"
          style={styles.input}
        ></TextInput>
        <TextInput
          label="Password"
          secureTextEntry
          mode="outlined"
          style={styles.input}
        ></TextInput>
        {/* login and signup buttons next to each other with react native paper */}
        <View style={{ flexDirection: "row" }}>
          <Button
            mode="contained"
            style={{ width: "50%", borderRadius: 5, margin: 10 }}
            onPress={() => console.log("Pressed")}
          >
            Login
          </Button>
          <Button
            mode="outlined"
            style={{ margin: 10, borderRadius: 5, borderColor: "#d1c5d1" }}
            onPress={() => console.log("Pressed")}
          >
            Signup
          </Button>
        </View>
        <StatusBar style="auto" />
      </View>
    </PaperProvider>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: "#fff",
    alignItems: "center",
    justifyContent: "center",
  },
  input: {
    width: "80%",
    margin: 10,
  },
  btn: { width: "50%", borderRadius: 5, margin: 10 },
});
